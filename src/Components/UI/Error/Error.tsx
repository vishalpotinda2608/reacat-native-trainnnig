import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Error = (props) =><Text style={{color:'red'}}>{props.title}</Text>

export default Error

const styles = StyleSheet.create({})